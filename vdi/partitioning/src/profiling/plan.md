
1. rank the top 2-3 tables both in value and in frequency
2. For each table, rank the top 2 columns both in value and frequency
3. Use DuckDB to create a hive partitioned table for each of those tables on the
   columns selected in step 2
4. Rewrite queries to use the new partitioned tables with sqlglot 


