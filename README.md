Repo for the Value of Data Indicator. 

Submodules for the forked and modified version of the Parquet-Java and Spark libraries which are used to build the Spark implementation (which is in the Spark fork), with code for instrumenting or running the VDI in the `vdi` folder. 
